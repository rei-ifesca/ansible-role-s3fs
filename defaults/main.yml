---
s3fs:
  buckets: []
  # Example
  #  - mountpoint: /mnt/s3fs
  #    bucket: s3fs
  #    accessKeyId: "accessKeyId"
  #    secretAccessKey: "secretAccessKey"
  #    options: "allow_other,use_cache=/tmp,max_stat_cache_size=100000,uid=33,gid=33,umask=002"
  add_to_fstab: True
  passwd_path: '/etc/s3fs/passwd'
  passwd_file: 'passwd-s3fs'
  daemon: '/usr/bin/s3fs'

